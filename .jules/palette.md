## 2026-01-22 - API Dashboard Pattern
**Learning:** API-only services often lack discovery. Adding a simple HTML dashboard at the root improves onboarding significantly. Also, always check for newlines when appending to existing files (like .gitignore) to avoid corruption.
**Action:** Use `echo -e "
..."` or ensure newline presence when appending to config files.
